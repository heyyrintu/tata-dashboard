# Environment Configuration
NODE_ENV=production
PORT=5000

# Frontend URL for CORS (comma-separated for multiple origins)
# Example: FRONTEND_URL=http://localhost:5173,http://yourdomain.com
FRONTEND_URL=http://localhost:5173

# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/tata-dashboard
# For secured MongoDB with authentication:
# MONGODB_URI=mongodb://admin:strong-password@localhost:27017/admin
tata-dashboard?authSource=admin

# Security Configuration
# Note: API authentication has been removed - API is now accessible to all requests from FRONTEND_URL
# CORS protection via FRONTEND_URL is the primary security mechanism

# Optional: IP Whitelist (comma-separated IP addresses)
# If set, only these IPs can access the API
# ALLOWED_IPS=192.168.1.100,10.0.0.50

# Outlook/Office 365 Email Configuration (Optional)
OUTLOOK_CLIENT_ID=a55d7dc4-a0fe-42ab-9da2-6dc251be9727
# IMPORTANT: Use the Secret VALUE (long string), NOT the Secret ID
# The Secret Value is shown only once when you create it in Azure Portal
OUTLOOK_CLIENT_SECRET=your-secret-value-here
OUTLOOK_TENANT_ID=acbbfe28-991c-4367-a289-a8d8c40de5ad
OUTLOOK_UPLOAD_EMAIL=insights@dronalogitech.com
OUTLOOK_ARCHIVE_FOLDER=Processed
OUTLOOK_POLL_INTERVAL=600000
# Poll interval in milliseconds (600000 = 10 minutes)

